-- Voldigoad HUB - Rayfield
-- Tabs: Executor, PVP Scripts, Farm Scripts, FPS Scripts

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Voldigoad HUB",
   LoadingTitle = "Voldigoad HUB",
   LoadingSubtitle = "by Voldigoad",
   ConfigurationSaving = { Enabled = false },
   Discord = { Enabled = false },
   KeySystem = false,
})

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")
local VirtualInputManager = game:GetService("VirtualInputManager")

local Player = Players.LocalPlayer
local RestoreButton = nil

-- ==================== SOCIALS ====================
local DiscordLink = "https://discord.gg/SXf5yy224"
local TikTokLink = "https://www.tiktok.com/@anosvoldigoad15075"

-- Variables for Deniel Attack
local FastAttackEnabled = false
local FastAttackRange = 5000
local Net = ReplicatedStorage.Modules.Net
local RegisterHit = Net["RE/RegisterHit"]
local RegisterAttack = Net["RE/RegisterAttack"]
local FastAttackConnection = nil

-- Floating V Button
local function CreateVButton()
    if RestoreButton then RestoreButton:Destroy() end
    local Gui = Instance.new("ScreenGui", CoreGui)
    Gui.Name = "VRestore"
    Gui.ResetOnSpawn = false
    Gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

    local Btn = Instance.new("TextButton", Gui)
    Btn.Size = UDim2.new(0, 50, 0, 50)
    Btn.Position = UDim2.new(0, 10, 0.5, -25)
    Btn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    Btn.Text = "V"
    Btn.TextSize = 28
    Btn.Font = Enum.Font.GothamBlack
    Btn.TextColor3 = Color3.fromRGB(160, 0, 255)
    Btn.BorderSizePixel = 0

    Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 12)

    local Stroke = Instance.new("UIStroke", Btn)
    Stroke.Color = Color3.fromRGB(160, 0, 255)
    Stroke.Thickness = 2

    Btn.MouseButton1Click:Connect(function()
        Rayfield:Toggle()
        Gui:Destroy()
        RestoreButton = nil
    end)

    local dragging, dragStart, startPos
    Btn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = Btn.Position
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            Btn.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
    end)

    RestoreButton = Gui
end

UserInputService.InputBegan:Connect(function(input, gp)
    if not gp and input.KeyCode == Enum.KeyCode.LeftControl then
        Rayfield:Toggle()
        if not Rayfield.WindowVisible and not RestoreButton then
            CreateVButton()
        elseif Rayfield.WindowVisible and RestoreButton then
            RestoreButton:Destroy()
            RestoreButton = nil
        end
    end
end)

-- ==================== DENIEL ATTACK ====================
local function AttackMultipleTargets(targets)
    pcall(function()
        if not targets or #targets == 0 then return end
        local allTargets = {}
        for _, targetChar in pairs(targets) do
            local head = targetChar:FindFirstChild("Head")
            if head then table.insert(allTargets, { targetChar, head }) end
        end
        if #allTargets == 0 then return end
        RegisterAttack:FireServer(0)
        RegisterHit:FireServer(allTargets[1][2], allTargets)
    end)
end

local function GetNPCsInRange(range)
    local myChar = Player.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if not myHRP then return {} end
    local list = {}
    for _, plr in pairs(Players:GetPlayers()) do
        if plr ~= Player and plr.Character then
            local hum = plr.Character:FindFirstChild("Humanoid")
            local hrp = plr.Character:FindFirstChild("HumanoidRootPart")
            if hum and hrp and hum.Health > 0 and (hrp.Position - myHRP.Position).Magnitude <= range then
                table.insert(list, plr.Character)
            end
        end
    end
    local enemiesFolder = workspace:FindFirstChild("Enemies")
    if enemiesFolder then
        for _, npc in pairs(enemiesFolder:GetChildren()) do
            local hum = npc:FindFirstChild("Humanoid")
            local hrp = npc:FindFirstChild("HumanoidRootPart")
            if hum and hrp and hum.Health > 0 and (hrp.Position - myHRP.Position).Magnitude <= range then
                table.insert(list, npc)
            end
        end
    end
    return list
end

local function StartFastAttack()
    if FastAttackConnection then task.cancel(FastAttackConnection) end
    FastAttackConnection = task.spawn(function()
        while FastAttackEnabled do
            task.wait(0.01)
            local targets = GetNPCsInRange(FastAttackRange)
            if #targets > 0 then AttackMultipleTargets(targets) end
        end
    end)
end

local function StopFastAttack()
    if FastAttackConnection then task.cancel(FastAttackConnection); FastAttackConnection = nil end
end

-- ==================== TABS ====================
local Executor_Tab = Window:CreateTab("Executor")
local PVP_Tab = Window:CreateTab("PVP Scripts")
local Farm_Tab = Window:CreateTab("Farm Scripts")
local FPS_Tab = Window:CreateTab("FPS Scripts")

-- EXECUTOR TAB
Executor_Tab:CreateSection("Bloxstrap Loader")
Executor_Tab:CreateButton({Name = "Execute Bloxstrap", Callback = function() Rayfield:Notify({Title = "Loading...", Content = "Starting Bloxstrap", Duration = 3}) task.spawn(function() pcall(function() getgenv().autosetup = {path = 'Bloxstrap', setup = true} loadstring(game:HttpGet('https://raw.githubusercontent.com/qwertyui-is-back/Bloxstrap/main/Initiate.lua'))() end) end) end})
Executor_Tab:CreateToggle({Name = "Auto Setup Bloxstrap", CurrentValue = false, Flag = "AutoBloxstrap", Callback = function(Value) if Value then task.spawn(function() pcall(function() getgenv().autosetup = {path = 'Bloxstrap', setup = true} loadstring(game:HttpGet('https://raw.githubusercontent.com/qwertyui-is-back/Bloxstrap/main/Initiate.lua'))() end) end) end end})

-- PVP TAB
PVP_Tab:CreateSection("⚔️ Deniel Attack")
PVP_Tab:CreateToggle({Name = "Deniel Attack", CurrentValue = false, Flag = "DenielAttackToggle", Callback = function(Value) FastAttackEnabled = Value if Value then StartFastAttack() Rayfield:Notify({Title = "Deniel Attack", Content = "✅ ENABLED", Duration = 2}) else StopFastAttack() Rayfield:Notify({Title = "Deniel Attack", Content = "❌ DISABLED", Duration = 2}) end end})
PVP_Tab:CreateSlider({Name = "Attack Range (studs)", Range = {100, 10000}, Increment = 100, CurrentValue = 5000, Flag = "AttackRangeSlider", Callback = function(Value) FastAttackRange = Value end})

PVP_Tab:CreateSection("🔫 PVP Tools")
PVP_Tab:CreateButton({
    Name = "Sapi Hub PVP",
    Callback = function()
        Rayfield:Notify({Title = "Sapi Hub", Content = "Loading...", Duration = 3})
        pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/land9678/VT/refs/heads/main/SapiHubFruitsPvp"))()
        end)
    end
})

-- ==================== DENIEL SCRIPT ====================
PVP_Tab:CreateSection("Deniel Script")

PVP_Tab:CreateButton({
    Name = "Load Deniel Interface",
    Callback = function()
        Rayfield:Notify({Title = "Deniel", Content = "Loading improved interface...", Duration = 3})

        local BG_MAIN = Color3.fromRGB(10, 0, 20)
        local ACCENT = Color3.fromRGB(160, 0, 255)

        getgenv().AbuseActive = false
        getgenv().SelectedSlot = Enum.KeyCode.Three

        local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
        local MainFrame = Instance.new("Frame", ScreenGui)
        MainFrame.Size = UDim2.new(0, 180, 0, 300)
        MainFrame.Position = UDim2.new(0.5, -90, 0.5, -150)
        MainFrame.BackgroundColor3 = BG_MAIN
        MainFrame.BorderSizePixel = 2
        MainFrame.BorderColor3 = ACCENT
        Instance.new("UICorner", MainFrame)

        local TitleBar = Instance.new("Frame", MainFrame)
        TitleBar.Size = UDim2.new(1, 0, 0, 32)
        TitleBar.BackgroundColor3 = Color3.fromRGB(20, 0, 40)
        TitleBar.BorderSizePixel = 0
        Instance.new("UICorner", TitleBar)

        local Title = Instance.new("TextLabel", TitleBar)
        Title.Size = UDim2.new(1, -30, 1, 0)
        Title.Position = UDim2.new(0, 8, 0, 0)
        Title.Text = "Deniel"
        Title.TextColor3 = ACCENT
        Title.Font = Enum.Font.GothamBold
        Title.TextSize = 14
        Title.BackgroundTransparency = 1
        Title.TextXAlignment = Enum.TextXAlignment.Left

        local Subtitle = Instance.new("TextLabel", MainFrame)
        Subtitle.Size = UDim2.new(1, 0, 0, 18)
        Subtitle.Position = UDim2.new(0, 0, 0, 32)
        Subtitle.Text = "Abuse Panel"
        Subtitle.TextColor3 = Color3.fromRGB(200, 150, 255)
        Subtitle.Font = Enum.Font.Gotham
        Subtitle.TextSize = 11
        Subtitle.BackgroundTransparency = 1

        local MinBtn = Instance.new("TextButton", TitleBar)
        MinBtn.Size = UDim2.new(0, 24, 0, 24)
        MinBtn.Position = UDim2.new(1, -28, 0, 4)
        MinBtn.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
        MinBtn.Text = "-"
        MinBtn.TextColor3 = Color3.new(1, 1, 1)
        MinBtn.Font = Enum.Font.GothamBold
        MinBtn.TextSize = 16
        Instance.new("UICorner", MinBtn)

        local ContentFrame = Instance.new("Frame", MainFrame)
        ContentFrame.Size = UDim2.new(1, 0, 1, -52)
        ContentFrame.Position = UDim2.new(0, 0, 0, 52)
        ContentFrame.BackgroundTransparency = 1

        local minimized = false
        MinBtn.MouseButton1Click:Connect(function()
            minimized = not minimized
            if minimized then
                MainFrame.Size = UDim2.new(0, 180, 0, 32)
                ContentFrame.Visible = false
                Subtitle.Visible = false
                MinBtn.Text = "+"
            else
                MainFrame.Size = UDim2.new(0, 180, 0, 300)
                ContentFrame.Visible = true
                Subtitle.Visible = true
                MinBtn.Text = "-"
            end
        end)

        local function Press(key)
            VirtualInputManager:SendKeyEvent(true, key, false, game)
            task.wait(0.015)
            VirtualInputManager:SendKeyEvent(false, key, false, game)
        end

        local function ExecuteAbuse()
            if not getgenv().AbuseActive then return end
            local char = Player.Character
            if not char then return end
            local hrp = char:FindFirstChild("HumanoidRootPart")
            local hum = char:FindFirstChild("Humanoid")
            if not hrp or not hum or hum.Health <= 0 then return end

            hrp.CFrame = hrp.CFrame * CFrame.new(0, 400, 0)
            task.wait(0.1)
            Press(getgenv().SelectedSlot)
            task.wait(0.15)
            Press(Enum.KeyCode.J)
            task.wait(0.12)

            local targetPos = CFrame.new(923.2, 3000000000000000000000, 32852.8)
            hrp.Anchored = true
            hrp.CFrame = targetPos
            workspace.CurrentCamera.CFrame = targetPos

            task.wait(0.08)
            Press(Enum.KeyCode.Z)

            task.spawn(function()
                task.wait(0.05)
                hum.Health = 0
            end)

            task.wait(0.6)
            hrp.Anchored = false
        end

        local function VoidSkill()
            local char = Player.Character
            if not char then return end
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if not hrp then return end

            local oldPos = hrp.CFrame
            hrp.Anchored = true
            hrp.CFrame = CFrame.new(923.2, 3000000000000000000000, 32852.8)
            workspace.CurrentCamera.CFrame = hrp.CFrame

            task.wait(0.15)
            Press(Enum.KeyCode.Z)
            task.wait(0.9)

            hrp.CFrame = oldPos
            hrp.Anchored = false
            if char:FindFirstChild("Humanoid") then
                workspace.CurrentCamera.CameraSubject = char.Humanoid
            end
        end

        local function MakeButton(parent, text, yPos, bgColor)
            local btn = Instance.new("TextButton", parent)
            btn.Size = UDim2.new(0.88, 0, 0, 38)
            btn.Position = UDim2.new(0.06, 0, 0, yPos)
            btn.BackgroundColor3 = bgColor
            btn.Text = text
            btn.TextColor3 = Color3.new(1, 1, 1)
            btn.Font = Enum.Font.GothamBold
            btn.TextSize = 12
            Instance.new("UICorner", btn)
            return btn
        end

        local Abuse3Btn = MakeButton(ContentFrame, "ABUSE 3",  10,  Color3.fromRGB(50, 0, 80))
        local Abuse1Btn = MakeButton(ContentFrame, "ABUSE 1",  58,  Color3.fromRGB(50, 0, 80))
        local VoidBtn   = MakeButton(ContentFrame, "INFINITE VOID", 106, Color3.fromRGB(25, 0, 50))
        local FixBtn    = MakeButton(ContentFrame, "FIX CAMERA", 175, Color3.fromRGB(20, 0, 35))
        FixBtn.TextColor3 = ACCENT
        FixBtn.Size = UDim2.new(0.88, 0, 0, 28)

        Abuse3Btn.MouseButton1Click:Connect(function()
            getgenv().SelectedSlot = Enum.KeyCode.Three
            getgenv().AbuseActive = not getgenv().AbuseActive
            Abuse3Btn.BackgroundColor3 = getgenv().AbuseActive and Color3.fromRGB(120, 0, 200) or Color3.fromRGB(50, 0, 80)
        end)

        Abuse1Btn.MouseButton1Click:Connect(function()
            getgenv().SelectedSlot = Enum.KeyCode.One
            getgenv().AbuseActive = not getgenv().AbuseActive
            Abuse1Btn.BackgroundColor3 = getgenv().AbuseActive and Color3.fromRGB(120, 0, 200) or Color3.fromRGB(50, 0, 80)
        end)

        VoidBtn.MouseButton1Click:Connect(VoidSkill)

        FixBtn.MouseButton1Click:Connect(function()
            getgenv().AbuseActive = false
            if Player.Character and Player.Character:FindFirstChild("HumanoidRootPart") then
                Player.Character.HumanoidRootPart.Anchored = false
                workspace.CurrentCamera.CameraSubject = Player.Character.Humanoid
            end
        end)

        task.spawn(function()
            while true do
                task.wait(0.1)
                if getgenv().AbuseActive then
                    pcall(ExecuteAbuse)
                end
            end
        end)

        Rayfield:Notify({Title = "Deniel", Content = "Interface loaded successfully ✅", Duration = 4})
    end
})

-- ==================== FARM TAB ====================
Farm_Tab:CreateSection("📜 Farm Scripts")
Farm_Tab:CreateButton({Name = "Hop Script", Callback = function() Rayfield:Notify({Title = "Hop Script", Content = "Loading...", Duration = 3}) pcall(function() loadstring(game:HttpGet("https://github.com/WhiteX1208/Scripts/blob/main/HopScript.luau?raw=true"))() end) end})
Farm_Tab:CreateButton({Name = "Main Loader", Callback = function() Rayfield:Notify({Title = "Main Loader", Content = "Loading...", Duration = 3}) pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/Omgshit/Scripts/main/MainLoader.lua"))() end) end})
Farm_Tab:CreateButton({Name = "Vudatdz Script", Callback = function() Rayfield:Notify({Title = "Vudatdz", Content = "Loading...", Duration = 3}) pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/hihiUGI/ubiquitous-octo-fortnight/refs/heads/main/vudatdz.txt"))() end) end})
Farm_Tab:CreateButton({Name = "Quantum Onyx", Callback = function() Rayfield:Notify({Title = "Quantum Onyx", Content = "Loading...", Duration = 3}) pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/flazhy/QuantumOnyx/refs/heads/main/QuantumOnyx.lua"))() end) end})
Farm_Tab:CreateButton({Name = "Xeter Hub", Callback = function() Rayfield:Notify({Title = "Xeter", Content = "Loading...", Duration = 3}) pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/TlDinhKhoi/Xeter/refs/heads/main/Main.lua"))() end) end})
Farm_Tab:CreateButton({Name = "Gravity Hub", Callback = function() Rayfield:Notify({Title = "Gravity Hub", Content = "Loading...", Duration = 3}) pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/Dev-GravityHub/BloxFruit/refs/heads/main/Main.lua"))() end) end})

-- FPS TAB
FPS_Tab:CreateSection("FPS Boosters")
FPS_Tab:CreateButton({Name = "Fix Lag", Callback = function() Rayfield:Notify({Title = "Fix Lag", Content = "Optimizing...", Duration = 2}) pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/TurboLite/Script/main/FixLag.lua"))() end) end})
FPS_Tab:CreateButton({Name = "Voldigoad Panel", Callback = function() Rayfield:Notify({Title = "Voldigoad Panel", Content = "Loading...", Duration = 2}) pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/JoshzzAlteregooo/FpsBoosterV2/refs/heads/main/FpsBoosterV2/JoshzzPanelBooster"))() end) end})
FPS_Tab:CreateButton({Name = "FPS Booster Pro", Callback = function() Rayfield:Notify({Title = "FPS", Content = "Optimizing...", Duration = 2}) pcall(function() loadstring(game:HttpGet("https://gist.githubusercontent.com/soxcy-script/5f30788aecf18271b4c13c11e806e6f9/raw"))() end) end})
FPS_Tab:CreateButton({Name = "FPS Booster Legacy", Callback = function() Rayfield:Notify({Title = "FPS", Content = "Optimizing...", Duration = 2}) pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/CasperFlyModz/discord.gg-rips/main/FPSBooster.lua"))() end) end})
FPS_Tab:CreateButton({Name = "Remove Textures", Callback = function()
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("BasePart") and v.Material ~= Enum.Material.Plastic then v.Material = Enum.Material.Plastic end
        if v:IsA("Decal") or v:IsA("Texture") then v.Transparency = 1 end
    end
    Rayfield:Notify({Title = "FPS", Content = "Textures removed", Duration = 2})
end})

-- ==================== UPDATES ====================
local Updates_Tab = Window:CreateTab("Updates")

Updates_Tab:CreateSection("📢 Updates & Socials")

Updates_Tab:CreateButton({
   Name = "📱 Official Discord",
   Callback = function()
        setclipboard("https://discord.gg/BwuguQmw2")
        Rayfield:Notify({Title = "Discord", Content = "Link copied ✅", Duration = 5})
   end,
})

Updates_Tab:CreateButton({
   Name = "🎵 TikTok",
   Callback = function()
        setclipboard("https://www.tiktok.com/@anosvoldigoad15075")
        Rayfield:Notify({Title = "TikTok", Content = "Link copied ✅", Duration = 5})
   end,
})

Rayfield:LoadConfiguration()
